<ul class="list-group" id="categories">
    {{#each categories}}
        <li class="list-group-item tablinks"onclick="openLabel(event, `{{title}}`)">
            <a href="#" id="{{title}}-link">{{title}}</a>
        </li>            
    {{/each}}
</ul>