<ul class="list-group" id="categories">
    {{#each categories}}
        <li class="list-group-item">
            <a href="#" id="{{title}}-link" class="tablinks" onclick="openLabel(event, {{id}})">{{title}}</a>
        </li>            
    {{/each}}
</ul>